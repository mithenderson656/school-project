fn main() {
    // Example usage of variables and basic arithmetic operations.
    let x = 5;
    let y = 3;

    let sum: i32 = x + y;
    let product = x * y;
    let quotient = x / y;

    println!("Sum: {}", sum);
    println!("Product: {}", product);
    println!("Quotient: {}", quotient);
}
